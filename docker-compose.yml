services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: andespace
      POSTGRES_PASSWORD: andespace
      POSTGRES_DB: andespace
    ports:
      - "5433:5432"
    volumes:
      - andespace_pg:/var/lib/postgresql/data

volumes:
  andespace_pg: